using PTXClassLibrary;

namespace PTX.ClassLibrary
{
    public class RetAge_Model : DBBase
    {
        // Add properties specific to RetAge_Model

        // Constructor
        public RetAge_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// Eqn_Model.cs


namespace PTX.ClassLibrary
{
    public class Eqn_Model : DBBase
    {
        // Add properties specific to Eqn_Model

        // Constructor
        public Eqn_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}




namespace PTX.ClassLibrary
{
}

// DefRev_Model.cs

namespace PTX.ClassLibrary
{
    public class DefRev_Model : DBBase
    {
        public string SomeDefRevProperty { get; set; }
        // Add other properties specific to DefRev_Model

        // Constructor
        public DefRev_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}


namespace PTX.ClassLibrary
{
    public class Frank_Model : DBBase
    {
        public string SomeFrankProperty { get; set; }
        // Add other properties specific to Frank_Model

        // Constructor
        public Frank_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// ERF_Model.cs


namespace PTX.ClassLibrary
{
    public class ERF_Model : DBBase
    {
        public string SomeERFProperty { get; set; }
        // Add other properties specific to ERF_Model

        // Constructor
        public ERF_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}
namespace PTX.ClassLibrary
{
    public class LRFModel : DBBase
    {
        public string SomeLRFProperty { get; set; }
        // Add other properties specific to LRFModel

        // Constructor
        public LRFModel(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}


namespace PTX.ClassLibrary
{
    public class DISModel : DBBase
    {
        // Add properties specific to DISModel

        // Constructor
        public DISModel(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// LEA_Model.cs

namespace PTX.ClassLibrary
{
    public class LEA_Model : DBBase
    {
        // Add properties specific to LEA_Model

        // Constructor
        public LEA_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// DiscBens_Model.cs


namespace PTX.ClassLibrary
{
    public class DiscBens_Model : DBBase
    {
        // Add properties specific to DiscBens_Model

        // Constructor
        public DiscBens_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// CO_Model.cs


namespace PTX.ClassLibrary
{
    public class CO_Model : DBBase
    {
        // Add properties specific to CO_Model

        // Constructor
        public CO_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// AccCease_Model.cs


namespace PTX.ClassLibrary
{
    public class
        AccCease_Model : DBBase
    {
        // Add properties specific to AccCease_Model

        // Constructor
        public AccCease_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// SalLink_Model.cs


namespace PTX.ClassLibrary
{
    public class SalLink_Model : DBBase
    {
        // Add properties specific to SalLink_Model

        // Constructor
        public SalLink_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// Underpin_Model.cs


namespace PTX.ClassLibrary
{
    public class Underpin_Model : DBBase
    {
        // Add properties specific to Underpin_Model

        // Constructor
        public Underpin_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}

// LS_Model.cs


namespace PTX.ClassLibrary
{
    public class LS_Model : DBBase
    {
        // Add properties specific to LS_Model

        // Constructor
        public LS_Model(string iD)
            : base(iD)
        {
            // Generate UID
            UID = $"{ID}_{VersionUID}";
        }

        public override void UpdateTypeAndID()
        {
            // Implement logic to update ErrMessage and ID if needed
        }
    }
}